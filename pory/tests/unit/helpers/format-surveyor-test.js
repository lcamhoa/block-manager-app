import { formatSurveyor } from '../../../helpers/format-surveyor';
import { module, test } from 'qunit';

module('Unit | Helper | format surveyor');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatSurveyor(42);
  assert.ok(result);
});
